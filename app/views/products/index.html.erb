<h1>Cart</h1>
<ul>
  <% cart.each do |item| %>
  <li><%= item %></li>
  <% end %>
</ul>

<%= form_tag add_path, method:'post' do %>
<%= text_field_tag :product %>
<%= button_tag "add to cart", type:'submit' %>
<% end %>
